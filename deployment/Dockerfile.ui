FROM python:3.11-slim

WORKDIR /code

# Install only Streamlit + requests (lightweight)
RUN pip install --no-cache-dir streamlit==1.36.0 requests==2.32.3

COPY app/ui /code/app/ui

# Non-root user
RUN useradd -m appuser && chown -R appuser:appuser /code
USER appuser

EXPOSE 8501

HEALTHCHECK --interval=30s --timeout=10s --start-period=30s --retries=3 \
    CMD python -c "import requests; requests.get('http://localhost:8501/_stcore/health').raise_for_status()"

CMD ["streamlit", "run", "app/ui/streamlit_app.py", \
     "--server.port=8501", \
     "--server.address=0.0.0.0", \
     "--server.headless=true", \
     "--server.enableCORS=false"]
